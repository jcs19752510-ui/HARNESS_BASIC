# 보안 취약점 신고 (Security Policy)

> ⚠️ 이 저장소(`HARNESS_BASIC`)는 실행되는 서비스가 아니라 문서/거버넌스
> 프레임워크 자체입니다(`CLAUDE.md` "프로젝트 개요" 참조). 아래 내용은
> 이 저장소 자체의 문제와, 이 템플릿으로 만든 실제 프로젝트의 문제를
> 구분해서 다룹니다.



---

## 1. 이 저장소(HARNESS_BASIC) 자체의 문제

`harness/` 문서 내용의 오류, `.claude/hooks`·CI 설정의 결함(예: 위험한
기본값, 우회 가능한 검증 로직)은 이 저장소의 GitHub Issues로 신고해
주세요. 즉시 악용 가능한 우회 방법처럼 공개 이슈로 올리기 부담스러운
내용이라면, 이슈 대신 [사람 작성 — 비공개 신고 연락처]로 신고해 주세요.

## 2. 이 템플릿으로 만든 실제 프로젝트의 취약점

`harness_10_data_lifecycle.md §6`(개인정보 유출 대응)이 "우리가 이미
사고를 인지했을 때"를 다루는 것과 달리, 이 절은 **외부 연구자/사용자가
취약점을 먼저 발견했을 때 어디로 신고해야 하는지**를 다룹니다. "Use this
template"으로 새 저장소를 만들면 이 파일의 아래 항목을 실제 값으로
채우세요 — 비워두면 신고를 받을 방법이 없는 상태로 서비스가 나갑니다.

- 신고 접수 채널: [사람 작성 — 예: security@example.com, 또는 GitHub
  Security Advisories 비공개 신고 기능]
- 응답 목표 시간: [사람 작성 — 예: 영업일 기준 2일 이내 최초 응답]
- 신고자 보호 원칙: [사람 작성 — 예: 선의로(비파괴적으로) 신고한 취약점에
  대해 법적 조치를 취하지 않는다는 확약 여부]
- 지원 버전: [사람 작성 — 어느 버전까지 보안 패치를 지원하는지]

**처리 절차**
1. 신고 접수 → `harness_11_raci.md` Security 역할에게 즉시 보고
2. 개인정보 노출이 확인되면 `harness_10_data_lifecycle.md §6`(유출사고
   대응 절차)로 전환 — 법정 신고기한이 있으므로 사람 보고 라인을
   최우선으로 가동
3. 수정 완료 후 `harness_09_deployment_runbook.md`(배포·롤백) 절차로 반영
4. Critical/Major 등급이면 `harness_09 §8` 포스트모템 작성
5. 재발 방지 가능한 일반화된 패턴이면 `harness_06_meta_improvement.md §3`
   실패 패턴 라이브러리에 등록
6. 신고자에게 수정 완료 사실과 공개(disclosure) 시점을 협의 후 통지
