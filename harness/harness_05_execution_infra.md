# 실행 인프라 계층 (Layer 2 — 규칙을 시스템으로 강제하기)

> 거버넌스 문서(00~04)가 "규칙을 적은 것"이라면, 이 문서는 그 규칙이
> 사람 기억에 의존하지 않고 구조적으로 지켜지게 만드는 실제 설정들입니다.

---

## 1. 버전관리 / 브랜치 전략

**원칙:** 실행 단위(TRD 1개) = 브랜치 1개. AC 통과 전에는 메인 브랜치에 못 들어온다.

```
main                        ← 항상 배포 가능한 상태만 유지
 └─ feature/{unit-code}      예: feature/a3-core-feature
```

- 브랜치명 규칙: `feature/{작업지시서 단위코드}`
- 커밋 메시지 규칙: `[{단위코드}] {한 일 요약}` — 예: `[A3] 핵심 기능 API 구현, AC-1~AC-4 통과`
- **머지 조건 (반드시 강제):**
  - AC 항목 전부 pass
  - 사람의 diff 리뷰 완료 (승인 없이는 머지 불가 — GitHub/GitLab의 "리뷰 필수" 브랜치 보호 규칙 사용). 리뷰 인원 수·강도는 `harness_07 §3-2`(위험도 점수 → 리뷰 강도 연동)를 따른다 — 모든 단위를 똑같은 강도로 검토하지 않는다.
  - CI(아래 2번) 통과
- 머지 후 즉시 A0 인수인계 문서 갱신 → 같은 커밋 또는 바로 다음 커밋에 포함

## 2. CI(자동 검증) 최소 구성

**원칙:** AC를 사람이 눈으로만 확인하지 않는다. 코드로 옮길 수 있는 AC는 전부 자동 테스트로 만든다.

TRD §6 테스트 시나리오 → 실제 테스트 코드로 변환하는 게 원칙입니다. 예시(GitHub Actions, 스택은 프로젝트에 맞게 교체):

```yaml
# .github/workflows/ci.yml (예시 — 실제 스택 확정 후 구체화)
name: CI
on: [pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 의존성 설치
        run: echo "프로젝트 스택 확정 후 채우기"
      - name: 테스트 실행 (AC 기반)
        run: echo "TRD §6 기반 테스트 스위트 실행"
      - name: 결과를 PR에 코멘트로 남기기
        run: echo "pass/fail 요약을 사람이 리뷰 전에 볼 수 있게"
```

- **왜 PR 단계에서 도는가:** 머지되기 전에, 사람이 리뷰하기 전에 실패를 먼저 걸러내기 위함.
- 테스트가 불가능한 AC(예: UX 판단성 항목)는 CI 대신 사람 체크리스트로 남기되, TRD §6에 "자동화 불가 — 수동 확인"이라고 명시.
- **실제로 이 저장소의 `.github/workflows/ci.yml`에는 위 예시 하나 외에도 이미
  구성된 잡이 있다**: `repo-health`(저장소 상태 검증), `secret-scan`(gitleaks),
  `dependency-scan`(의존성 취약점 스캔 — `harness_13 §7` 참조), `lint`(정적분석/
  포맷팅/타입체크 — `harness_13 §4` 참조). 프로젝트 스택이 정해지면 `test`·
  `dependency-scan`·`lint` 세 잡의 내용만 채우면 된다.

## 3. 에이전트 실행 환경 / 권한 정책

**원칙:** AI가 실수로라도 스코프 밖에 영향을 못 미치게, 문서가 아니라 환경 자체를 제한한다.

- **파일시스템 범위:** 에이전트는 해당 단위의 작업 디렉토리 밖(다른 단위 코드, 설정파일 등)에 쓰기 권한을 주지 않는다. 필요하면 읽기 전용으로만 허용.
- **네트워크/외부 접근:** 프로덕션 DB, 실제 운영 API 키, 실제 사용자 개인정보는 에이전트 환경에 절대 넣지 않는다. 개발/샘플 DB만 연결.
- **민감정보 마스킹:** 개인정보 등 민감데이터는 실제 값 대신 샘플/마스킹 데이터를 사용한 시드(seed) 데이터셋을 별도로 준비해서 제공.
- **위험 명령 차단:** DB 스키마 DROP, 대량 삭제, 배포 명령 등은 에이전트가 직접 실행하지 못하게 하고, 사람이 승인 후 별도로 실행.
- **리뷰 컨텍스트 분리:** Phase B-6 "사람 검증"의 AI 보조 리뷰를, 코드를 작성한
  바로 그 세션이 스스로 수행하지 않는다 — 같은 컨텍스트는 확증편향에서 자유롭기
  어렵다. "환경 자체를 제한한다"는 이 절의 원칙을 리뷰 단계에도 적용해, 별도
  컨텍스트(새 세션 또는 Write/Edit/Bash 권한이 없는 서브에이전트)가 리뷰를
  맡도록 한다. **(선택) Claude Code 사용자**: `.claude/agents/harness-reviewer.md`
  가 Read/Grep/Glob만 가진 리뷰 전용 서브에이전트입니다 — 다른 도구를 쓰면
  "새 대화창/새 세션에서 리뷰"만 지켜도 같은 효과를 냅니다.
- **자격증명(신원) 관리 — Credential Lifecycle** [원칙은 고정, 세부 정책은
  사람 작성 — `harness_05 §6` OWASP 대조표 ASI03(신원·권한 남용) 갭 해소용]:
  범위는 AI 에이전트/자동화 프로세스가 개발·실행 중에 쓰는 자격증명(MCP
  서버 토큰, CI 시크릿, 저장소 접근 토큰 등)에 한정한다 — 하위 프로젝트의
  실제 운영 서비스 계정(프로덕션 DB 등)은 `harness_09`·`harness_10`·
  `harness_20`에서 별도로 다룬다.
  - 개별 신원 원칙: 에이전트 세션·자동화 프로세스(CI 잡 포함)는 사람 개인
    계정이나 다른 세션과 공유하는 자격증명을 재사용하지 않는다 — 공유하면
    (1) 어느 세션이 특정 행동을 했는지 감사가 불가능해지고, (2) 하나가
    유출되면 그 자격증명을 쓰는 모든 세션이 동시에 위험해진다. (예: 이
    저장소 CI의 `GITHUB_TOKEN`은 워크플로우 실행마다 GitHub이 자동으로
    별도 발급·만료시키므로 이미 이 원칙을 따르고 있다 — `§2` 참조.)
  - 최소 권한 원칙: 각 신원은 그 작업에 필요한 최소 권한 범위만 부여한다
    (예: 저장소 전체 쓰기 권한 대신, 가능하면 특정 저장소·특정 동작으로
    범위를 좁힌 토큰 사용).
  - 단기 수명 우선: 가능하면 영구 토큰보다 짧은 수명(세션/시간 단위) 토큰을
    쓴다. 장기 고정 토큰이 불가피하면 그 이유를 기록하고 아래 회전 주기를
    반드시 명시한다.
  - 저장 위치: `harness_13 §1` "저장소 접근 방법" 원칙을 그대로 따른다 —
    저장소 안(gitignore 여부와 무관) 어디에도 실제 토큰/키 값을 두지 않고
    OS 자격증명 관리자·시크릿 매니저에만 둔다.
  - 회전 주기 [사람 작성]:
  - 이상행동 의심 시 즉시 폐기 절차 및 담당자 [사람 작성 — `harness_11
    RACI` "자격증명 발급/폐기 승인" 행 참조]:
  - 감사 로그: `§5`(관찰가능성) 로그에 "어느 신원(에이전트/토큰)이
    사용됐는지"를 포함한다 — 행동만 기록하고 누가/무엇으로 했는지가
    없으면 사고 후 원인 추적이 불가능하다.
- **지속 메모리(Persistent Memory) 신뢰 검증 — Memory Trust Verification**
  [원칙은 고정, 세부 절차는 사람 작성 — `§6` OWASP 대조표 ASI06(메모리·
  컨텍스트 오염) 갭 해소용]: 범위는 개발에 쓰는 AI 코딩 에이전트가 세션
  간 정보를 이어가기 위해 쓰는 지속 메모리 기능(예: 세션-간 자동 저장
  메모리)에 한정한다 — 하위 프로젝트가 제품 기능으로 구현하는 에이전트
  메모리(RAG, 벡터DB, 대화 이력 기반 개인화 등)는 대상 밖이며, 해당
  프로젝트 TRD §0-1에서 별도로 검토한다.
  - 초안 취급 원칙: 자동 저장된 메모리 항목도 `CLAUDE.md` 원칙 8("AI
    초안, 사람 검토 필요")의 적용 대상이다 — 메모리에 기록됐다는 사실
    만으로 검증된 사실처럼 취급해 이후 행동의 근거로 삼지 않는다.
  - 출처 구분 원칙: 메모리에 새로 기록되는 내용이 (a) 사람이 대화 중
    직접 확인·지시한 내용, (b) AI가 세션 중 스스로 추론·요약한 내용,
    (c) 외부 콘텐츠(문서/웹페이지/도구 출력 등)에서 유입된 내용 중
    어디서 왔는지 구분 가능해야 한다 — 구분이 없으면 외부 콘텐츠에
    숨겨진 지시가 메모리를 거쳐 "이미 확인된 사실"처럼 조용히 고착되는
    경로를 막을 수 없다(`harness_00_overview` 원칙 9 간접 프롬프트
    인젝션 방어와 동일한 위협 모델).
  - 정기 검토 주기 [사람 작성]:
  - 이상(스코프 밖 지시, 출처 불명 내용 등) 발견 시 폐기·롤백 절차 및
    담당자 [사람 작성 — `harness_11 RACI` "지속 메모리 이상 감지 시
    폐기 승인" 행 참조]:
- **AI 코딩 에이전트 행동 기준선 및 즉시 중단(Kill Switch)** [원칙은
  고정, 세부 기준값은 사람 작성 — `§6` OWASP 대조표 ASI10(불량 에이전트)
  갭 해소용]: 범위는 이 하네스로 개발할 때 실제로 코드를 작성·실행하는
  AI 코딩 에이전트(세션) 자체의 이상행동 감지에 한정한다 — 하위
  프로젝트가 배포하는 자율 에이전트 제품 기능(예: 자동화 업무봇)의
  이상행동 감시는 대상 밖이며, 해당 프로젝트는 `harness_17_monitoring.md`
  에서 별도로 다룬다.
  - 즉시 중단 권한: 에이전트가 스코프 밖 파일에 반복 접근을 시도하거나,
    `§4` 에스컬레이션 없이 위험 명령을 반복 요청하거나, 동일 실패를
    재시도 근거 없이 반복하는 등 이상 징후가 보이면, 세션을 지켜보는
    사람이 별도 승인 절차 없이 즉시 세션을 중단할 권한을 갖는다(사후
    `harness_11 RACI` "AI 에이전트 이상행동 세션 중단 확인" 행에 기록).
  - 행동 기준선("무엇이 정상 행동인지") [사람 작성 — 프로젝트/도구별
    구체값]:
  - 반복 발생 시 `harness_16_tech_debt_log.md` 또는 `harness_06 §3`
    실패 패턴 라이브러리에 등록해 재발 방지책으로 축적한다.

## 4. 에스컬레이션 프로토콜

**원칙:** "정책 불명확하면 멈추고 보고하라"는 규칙이 실제로 작동하려면, 멈춘 다음에 무슨 일이 일어나는지가 정의돼 있어야 한다.

| 상황 | 에이전트가 할 일 | 사람이 할 일 |
|---|---|---|
| TRD §7/작업지시서 §3의 "별도 확인 필요" 항목에 실제로 부딪힘 | 해당 지점에서 진행 중단, 무엇이 불명확한지 구체적으로 기록 | 그 항목에 대해 결정 → 작업지시서 §3 갱신 → 세션 재개 |
| TRD와 실제(코드/데이터)가 다름을 발견 | 임의로 어느 쪽을 따를지 정하지 말고 양쪽을 보고 | 어느 쪽이 맞는지 판단 → TRD 자체를 수정할지 결정 |
| AC를 도저히 충족 못 시키는 기술적 제약 발견 | 왜 불가능한지 근거와 함께 보고 | AC 자체를 재협상하거나 다른 접근법 지시 |

- **응답 기한 원칙:** 사람이 응답하기 전까지 해당 브랜치는 진행하지 않는다 (막힌 채로 다음 것을 임의로 진행시키지 않음).

## 5. 관찰 가능성(Observability) — 로그 보관

**원칙:** A0 문서는 사람이 요약한 결과다. 나중에 "왜 이렇게 됐지"를 재구성하려면 원본이 필요하다.

- 각 실행 단위 세션의 원본 로그(에이전트가 실행한 명령, 응답)를 `logs/{단위코드}_{날짜}.log`로 보관
- 최소한 다음은 반드시 남긴다: 최종 프롬프트 전문, 실패했던 시도와 재시도 이유, 최종 diff
- 로그는 커밋하지 않고 별도 저장(용량 문제) — 단, A0 문서에는 로그 파일 경로를 링크
- 사용한 AI 에이전트/모델명과 버전을 로그에 함께 기록한다 (`harness_04` 모델
  버전 변경 점검과 연동).

## 6. OWASP Top 10 for Agentic Applications 대조표 (2026)

**원칙:** 위협에 대한 원칙을 하나씩 "그럴듯해서 추가"하는 방식으로는 빠짐없이
커버했는지 확인할 방법이 없다. OWASP GenAI Security Project가 2025-12-09
발표한 "OWASP Top 10 for Agentic Applications 2026"(보안 전문가 100인
이상 동료 검토)을 공통 기준점으로 삼아, 하네스의 어느 장치가 각 위협을
방어하는지 명시적으로 대조한다. **새 프로젝트 착수 시 이 표부터 훑고,
"미대응" 항목이 해당 프로젝트에 실제로 위험한지 재검토한다.**

| 코드 | 위협 | 한 줄 설명 | 대응 현황 | 근거 / 미대응 시 최소 조치 |
|---|---|---|---|---|
| ASI01 | 목표 하이재킹 | 에이전트가 읽는 콘텐츠(이메일·문서 등)에 숨겨진 지시로 목표를 조작 | ✅ 대응 | `harness_00_overview.md` 원칙 9(간접 프롬프트 인젝션 방어) |
| ASI02 | 도구 오용 | 모호한 지시·과도한 권한으로 합법적 도구가 의도와 다르게 쓰임 | 🟡 부분 대응 | `§3` 위험 명령 차단(Bash) + `harness_13 §1` MCP 도구 지속 점검. 애플리케이션 자체 도구(내부 API 등) 오용 방지는 프로젝트별 TRD §0-1에서 별도 검토 필요 |
| ASI03 | 신원·권한 남용 | 공유 자격증명·장기 토큰의 과도한 권한 범위를 공격자가 상속 | 🟡 부분 대응 (2026-09-09 원칙 신설) | `§3` "자격증명(신원) 관리" 신설 — 개별신원/최소권한/단기수명/저장위치 원칙은 고정됐으나, 회전 주기·폐기 담당자 등 실제 값은 프로젝트마다 `[사람 작성]`으로 채워야 완전한 대응이 된다 |
| ASI04 | 공급망 취약점 | 프레임워크·MCP서버·도구 레지스트리의 보안 미흡, 배포 후 런타임에 새 도구가 등록됨 | 🟡 부분 대응 | `harness_13 §7`(패키지 취약점 스캔) + `.github/dependabot.yml`이 패키지 공급망은 커버. "런타임에 동적으로 등록되는 도구"는 대상 밖 |
| ASI05 | 예기치 않은 코드 실행 | 자연어 입력이 서브프로세스·인터프리터로 이어져 의도 밖 코드 실행 | ✅ 대응 | `§3` 위험 명령 차단 + `harness_00_overview` 원칙 3(사람 diff 리뷰 필수) |
| ASI06 | 메모리·컨텍스트 오염 | 지속 메모리에 악성 정보가 주입돼 세션 간 은밀하게 행동 조작 | 🟡 부분 대응 (2026-09-09 원칙 신설) | `§3` "지속 메모리 신뢰 검증" 신설 — 초안취급·출처구분 원칙은 고정됐으나, 정기 검토 주기·폐기 담당자 등 실제 값은 프로젝트마다 `[사람 작성]`으로 채워야 완전한 대응이 된다. 하위 프로젝트의 제품 메모리 기능(RAG 등)은 대상 밖 |
| ASI07 | 에이전트 간 통신 취약 | 다중 에이전트 시스템에 인증·무결성 검증 부재 | 🟡 부분 대응 (2026-09-09 원칙 신설) | `harness_15_parallel_work.md §6` "에이전트 간 통신 신뢰 원칙" 신설 — 서브에이전트 산출물 미검증 취급·권한위임 최소화 원칙은 고정됐으나, 신원 식별 규칙·통신 무결성 검증 절차는 `[사람 작성]`. 하위 프로젝트의 다중 에이전트 제품 아키텍처는 대상 밖 |
| ASI08 | 연쇄 실패 | 단일 실패가 연결된 워크플로우를 통해 전파·증폭 | 🟡 부분 대응(구조적) | 대규모 자동 fan-out을 기본으로 하지 않고 순차 단위 실행이 원칙(`harness_00` Phase B)이라 구조적으로 위험이 낮음 — 명시적 "실패 전파 차단" 장치는 없음 |
| ASI09 | 인간-에이전트 신뢰 악용 | 에이전트가 표시 정보를 통제해 승인 절차를 조작, 자신감 있는 요약을 악용 | ✅ 대응 | CLAUDE.md 원칙 8("검증 안 된 신입의 초안"), `harness-reviewer` 서브에이전트(코드 작성 세션과 분리된 독립 리뷰) |
| ASI10 | 불량 에이전트 | 탈취된 에이전트가 정상처럼 보이며 지속, 행동 기준선·킬스위치 필요 | 🟡 부분 대응 (2026-09-09 원칙 신설) | `§3` "AI 코딩 에이전트 행동 기준선 및 즉시 중단" 신설 — 즉시 중단 권한은 고정됐으나, 구체적인 행동 기준선 값은 `[사람 작성]`. 배포된 자율 에이전트 제품의 이상행동 감시는 `harness_17`에서 별도 |

> ASI03/06/07/10 4건은 2026-09-09에 개발/실행 인프라 레벨 원칙을 각각
> 신설해 ❌ 미대응 → 🟡 부분 대응으로 전환했다. 다만 4건 모두 회전
> 주기·검토 담당자·행동 기준선 등 프로젝트별 구체 값은 `[사람 작성]`으로
> 남아 있어, 그 값을 채워야 비로소 완전한 대응이 된다. 이 하네스가
> "순차 단위 실행 + 사람 승인 게이트" 모델을 기본으로 하는 한 발생
> 확률 자체는 낮지만, 프로젝트가 대규모 다중 에이전트 병렬 실행
> (`harness_15`)이나 자동 메모리 기능을 적극 쓰기 시작하면 이 빈칸을
> 채우는 우선순위가 급격히 올라간다. Phase A 착수 시 이 표를 훑고,
> `[사람 작성]` 빈칸이 남아있는 항목을 `harness_16_tech_debt_log.md`에
> 등록해 추적한다.
