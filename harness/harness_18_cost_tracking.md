# AI 세션 비용/시간 추적 (Cost & Time Tracking)

> 목적: `harness_06` 회고는 "시간을 절약했는가"를 정성적으로만 묻습니다.
> 이 하네스가 실제로 효율적인지, 어느 단계에서 재시도가 많이 발생해 비용이
> 새는지를 판단하려면 숫자가 있어야 합니다. 숫자가 없으면 하네스 개선(v1.1,
> v1.2...)도 감으로만 하게 됩니다.

---

## §1. 세션 기록

| 단위코드 | 세션 일시 | 소요시간 | 프롬프트 재시도 횟수 | AC 1차 통과율 | 비고 |
|---|---|---|---|---|---|
| A1(구현) | 2026-09-04 | 미기록(원본 로그 없음) | 최소 3회(코드리뷰 반영 커밋 기준) | 11/11 (1차부터 pass, 재시도는 리뷰 반영 단계) | 실시간 기록 습관이 없었던 첫 사례 — 이후 이 표를 실제로 쓰기 시작한 계기가 됨 |
| META(하네스 재감사+복구) | 2026-09-05 | 약 3시간(대화 기준 추정) | 0 (재시도 아님, 신규 발견 대응) | 해당없음(감사 세션) | dangling commit 복구, PROD 정식 병합, ADR-008, A7 범위 확정, PR템플릿/CI/pre-commit 실제 설치까지 포함 |

**이 표를 채우기 시작한 이유(2026-09-05)**: "하네스가 시간을 아꼈는가"에 숫자로
답한 적이 한 번도 없었다는 게 20년차 감사에서 지적됨(§4 참조). 지금부터라도
매 세션 끝에 이 표 한 줄만 채우면, 3~5개 프로젝트 뒤엔 실제 비교가 가능해짐.

## §2. 재시도 사유 분류
- 스펙 모호성 때문 → `harness_01 TRD §5 AC` 작성 품질 문제
- AI가 스펙을 잘못 이해 → 프롬프트(`harness_04`) 개선 필요
- 실제 기술적 난이도 → 정상적 재시도

## §3. 절차

```mermaid
flowchart LR
    A[단위 세션 종료] --> B[소요시간/재시도 기록]
    B --> C[재시도 사유 분류]
    C --> D[프로젝트 마일스톤마다 집계]
    D --> E[harness_06 회고 §2에 정량 데이터로 반영]
    E --> F[반복되는 비효율 - harness_04 블록1 상시규칙 후보로 승격]
```

## §4. 예방된 비용 추정 (카운터팩추얼 — "하네스 없었다면" 추정)

> §1은 "하네스를 쓰는 데 든 비용"만 보여준다. 그것만 보면 하네스가 늘 손해처럼
> 보인다(문서 쓰는 시간이 그냥 시간이니까). 진짜 비교는 "하네스가 없었다면
> 이 사고가 어떻게 끝났을까"까지 봐야 한다. 매번 정밀 계산은 불가능하니,
> 사고가 발생했을 때 "완전 손실 추정치"와 "실제 손실"을 나란히 적어 둔다.

| 날짜 | 사고 | 하네스 없었다면(추정) | 실제 손실 | 아낀 것 |
|---|---|---|---|---|
| 2026-09-05 | feature/a1-login 삭제 후 dangling commit | A1 전체 재구현(로그인+세션+CSRF+테스트 12개, 코드리뷰 7건 재발견) — 최소 며칠 | 감사/복구 세션 약 3시간, 코드 손실 0 | 코드베이스 전체 + 재발견 필요했을 버그 7건 |
| 2026-09-05 | PII 정책 미결인 채 데이터모델만 확정 | AI가 임의로 마스킹 없는 화면을 구현했을 가능성 → A2 이후 발견 시 화면/DB 재작업 | 정책만 지금 정하고 아직 화면 미착수(재작업 비용 0) | A2 구현 후 재작업 비용 전액 |
